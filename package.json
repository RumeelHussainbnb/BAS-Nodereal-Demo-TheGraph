{
  "name": "wbnb",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ bas/wbnb",
    "create-local": "graph create --node http://localhost:8020/ bas/wbnb",
    "remove-local": "graph remove --node http://localhost:8020/ bas/wbnb",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bas/wbnb"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
